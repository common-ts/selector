{
  "name": "reselect-plus",
  "version": "0.0.4",
  "description": "reselect",
  "main": "./lib/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build:lib": "tsc",
    "clean:lib": "rimraf lib"
  },
  "devDependencies": {
    "tslint": "5.10.0",
    "typescript": "^3.3.3333"
  },
  "peerDependencies": {
    "reselect": "^3.0.1"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com/common-ts/selector"
  },
  "keywords": [
    "reselect",
    "reselect-plus"
  ],
  "dependencies": {
    "reselect": "^3.0.1"
  }
}
